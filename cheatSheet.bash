# Estou usando o python 3.11.12 para os testes atuais
# Se os comandos em bash não rodarem automáticamente, execute manualmente

virtualenv .venv --python=python3.11
source .venv/bin/activate
pip install -r requirements.txt
# Vai demorar, boa sorte